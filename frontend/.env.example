# Frontend Environment Variables

# The base URL for the backend API in production.
# Leave empty for local development (will use Vite proxy).
# VITE_API_URL=https://your-production-backend.onrender.com/api
